public class DatabaseTest {

	//pomysły na testy:

	//mssqlproductdao:
	//add product - existing, non existing, wrong baseprice
	//get product by name - existing, non existing
	//updateproductbaseprice - existing, non existing, wrong baseprice, gut baseprice
	//updateproduct category - existing, non existing
	// remove rpoduct - existing, non- existing

	//mssqlstatedao:
	//get usstate by name - existing, non existing
	//edit category base tax - existing, non existing, wrong tax, gut tax
	//edit category value without tax - existing, non existing, wrong value, gut value
	//select taxes for state - existing, non existing

	//database
	// z mockiem już było (simple usstate dao, teraz z bazą
}
